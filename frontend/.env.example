# Environment Variables Template
# Copy to .env.local or set in deployment service environment settings (Render, Vercel, Netlify)

# Optional: Base URL of backend API (leave empty if backend serves static frontend or uses proxy)
# Example: VITE_API_URL=https://military-asset-backend.onrender.com/api
VITE_API_URL=
